# Scripts

This folder contains OSINT automation scripts, data broker opt-out utilities, and inbox metadata analyzers.

Planned scripts:
- `osint_scan.py` to drive the 500+ directory scanning pipeline.
- `broker_optout.py` for people-search workflows.
- `inbox_metadata.py` for OAuth-based mailbox analysis.
